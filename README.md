# Flood-prone Area Dashboard

The working Next.js app lives in [`web/`](web/).

```bash
cd web
npm install
npm run seed
npm run dev
```

See `web/README.md` for environment variables, schema, usage notes, and the Google Sheet importer.
